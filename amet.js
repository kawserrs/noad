// Get the first stop of the gradient
const firstStop = gradient.stops[0];

// Use the color of the first stop
const color = firstStop.color;

// Now you can use the color in your code
console.log(color);
